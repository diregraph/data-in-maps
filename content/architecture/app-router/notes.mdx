# How should the Next.js App Router be set up for an open-source map platform? What pages, route groups, and API routes are needed?

## Overview

_Placeholder — describe the overall App Router structure, route groups, and the rationale for each._

## Folder structure

```
src/app/
├── (marketing)/         # public-facing static pages
│   ├── layout.tsx
│   └── architecture/
├── (map)/               # interactive map shell
│   ├── layout.tsx
│   └── page.tsx
└── api/
    └── ...              # API routes
```

## Key decisions

- _Placeholder — e.g. why route groups are used, streaming vs. static rendering, middleware scope._

## Interfaces

```ts
// Placeholder — shared types consumed by layout and page components
```

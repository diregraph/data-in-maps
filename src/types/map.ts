// Global TypeScript contracts for map domain.
// Canonical types live in src/modules/map/types.ts; re-export here for cross-module use.
export type { Viewport, MapState, MapRef } from "@/modules/map/types"
